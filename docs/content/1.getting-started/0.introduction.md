---
title: 'Getting Started with Nuxt SEO'
description: 'Install Nuxt SEO and get sitemaps, robots.txt, meta tags, Schema.org, and OG images in one module. Zero config, sensible defaults.'
navigation:
  title: 'Introduction'
relatedPages:
  - path: /docs/nuxt-seo/getting-started/installation
    title: Installation
  - path: /docs/nuxt-seo/guides/using-the-modules
    title: Module Guide
  - path: /learn-seo/nuxt
    title: Nuxt SEO Guide
---

Nuxt SEO gives you sitemaps, robots.txt, meta tags, Schema.org, and OG images in one install. Zero config, sensible defaults.

```bash
npx nuxi module add @nuxtjs/seo
```

New to SEO? See the [Nuxt SEO Guide](/learn-seo/nuxt) first.

## Modules

:AllModules

Install all at once with `@nuxtjs/seo`, or pick individual modules.

## Nuxt SEO Module: @nuxtjs/seo

:ModuleCard{slug="nuxt-seo" class="w-1/2"}

The `@nuxtjs/seo` module is an alias that installs all the other SEO modules. It contains no logic of its own.

```ts
// This is all it does!
export default defineNuxtModule<ModuleOptions>({
  moduleDependencies: {
    '@nuxtjs/robots': {
      version: '>=5.5',
    },
    '@nuxtjs/sitemap': {
      version: '>=7.4',
    },
    'nuxt-link-checker': {
      version: '>=4.3',
    },
    'nuxt-og-image': {
      version: '>=6.0',
    },
    'nuxt-schema-org': {
      version: '>=5.0',
    },
    'nuxt-seo-utils': {
      version: '>=7.0',
    },
    'nuxt-site-config': {
      version: '>=3.2',
    },
    '@nuxtjs/i18n': {
      version: '>=10.0',
      optional: true,
    },
  },
})
```

### @nuxtjs/seo vs Individual Modules

Both approaches give you the exact same modules with the exact same features. The only difference is how you install them.

- **Use `@nuxtjs/seo`** if you want all modules with a single install. This is the easiest way to get started.
- **Install individually** if you only need specific modules (e.g. Sitemap and Robots) or want to pin each module to a specific version.

::callout{icon="i-heroicons-information-circle"}
Configuration, composables, and Nuxt Content integration work identically regardless of which approach you choose. All module documentation applies to both approaches.
::

::callout{icon="i-heroicons-wrench" to="/tools"}
**Debug your SEO**: Use our free [SEO Tools](/tools) to validate meta tags, test robots.txt, debug social cards, and validate structured data.
::

## Features

- **Zero config**: sensible defaults by default
- **Modules work together**: Site Config shares URL, name, and metadata across all modules
- **DevTools integration**: debug and preview SEO output
- **I18n ready**: full multilingual support with Nuxt I18n
- **Edge compatible**: [Vercel](https://vercel.com), [Netlify](https://netlify.com), [Cloudflare](https://cloudflare.com), and more

## Site Config

All modules need your site URL for canonical links and sitemaps. Set it once:

```ts
export default defineNuxtConfig({
  site: {
    url: 'https://example.com',
    name: 'My Site'
  }
})
```

Every module uses these values. No duplication.

:ModuleCard{slug="site-config" class="w-1/2"}
